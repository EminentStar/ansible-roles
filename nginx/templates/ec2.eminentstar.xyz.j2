server{
        listen 80;
        server_name {{ '{{' }} domain {{ '}}'  }};

        location /static {
                alias /home/vagrant/static;
        }

        location / {
                proxy_pass http://localhost:8000;
        }

}
